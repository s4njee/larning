{
  "name": "larning-study-guides",
  "private": true,
  "description": "Build-time tooling for the study-guide site. The guides are Markdown compiled to HTML by the Python build scripts; the only Node dependency is the mermaid renderer used to turn ```mermaid fenced blocks into inline SVG at build time (see build_guide.py).",
  "scripts": {
    "diagrams": "echo 'mermaid diagrams render automatically during: python3 build_all_guides.py'"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^11"
  }
}
