# Study Guides

This repository collects practical study guides for cloud, frontend, systems, and security topics. Each guide is written to be usable on its own, with the goal of helping you learn the subject by connecting concepts to real-world workflows and examples.

## Website

The generated self-contained website lives in `html/`, with `html/index.html` as the table of contents. GitHub Pages is deployed from that folder by `.github/workflows/pages.yml`, and the workflow rebuilds every guide before publishing so the index is not stale.

To rebuild locally:

```bash
python3 build_all_guides.py
```

## Guides

### Advanced Go
This guide is a depth-first treatment of the Go runtime and high-performance Go for engineers who already write Go and want to understand what happens beneath their code. Its thesis ties it to its sibling guides: in Python you fight the interpreter and in Node you fight the event loop, but in Go — already compiled to native code — you fight the allocator and the garbage collector. It covers the runtime model (native compilation, the embedded runtime, value semantics), the GMP scheduler (goroutines, work-stealing, the netpoller, async preemption, and container-aware `GOMAXPROCS`), escape analysis and allocation (stack vs heap, `-gcflags=-m`, interface boxing — the single highest-return lever), the garbage collector (concurrent low-latency mark-sweep, `GOGC` and `GOMEMLIMIT`), data structures and memory layout (slice preallocation, `[]T` vs `[]*T`, struct field packing, `strings.Builder`, cache locality), and concurrency for performance (channels vs mutexes vs atomics, contention, `sync.Pool`, bounded `errgroup`, goroutine leaks, the race detector). It then covers the compiler and PGO (inlining, devirtualization, a free 2–14% from profile-guided optimization, and why cgo is *slower*), profiling (`pprof` flame graphs, `b.Loop` benchmarks with `-benchmem`, `benchstat`, the execution tracer), the full ladder of performance levers, and ten worked recipes — preallocation, `sync.Pool`, bounded fan-out, atomic counters, allocation hunting, faster JSON, buffered I/O, container runtime tuning, PGO, and sharded maps — plus a decision tree.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/ADVANCED_GO_STUDY_GUIDE.md)

### Advanced Linux
This guide is a depth-first treatment of the tunable, observable, optimizable internals of Linux — the companion to the Linux Fundamentals guide, which covers the substrate (processes, file descriptors, signals, namespaces, cgroups); this guide covers what you tune, trace, and harden on top of it. It starts with the scheduler (CFS and its vruntime red-black tree, EEVDF in kernel 6.6+ with virtual deadlines and the sched_ext eBPF hook, scheduling classes, nice values, CPU affinity and isolcpus, and the full mechanics of CFS bandwidth control — how Kubernetes CPU limits cause throttling even on idle nodes, and why checking cpu.stat for nr_throttled is the first move for container CPU issues). It then goes deep on memory management (virtual memory and page tables, TLB and its performance impact, demand paging and copy-on-write, huge pages — transparent vs explicit and the THP controversy for databases, the page cache and the classic "no free memory" misunderstanding, memory reclaim and swappiness tuning, the OOM killer's scoring system and how Kubernetes sets oom_score_adj by QoS class, the cgroups v2 memory controller in full — memory.high throttling vs memory.max killing — and PSI pressure stall information), the I/O stack (the full path from write() to device, buffered vs direct I/O, durability guarantees — fsync/fdatasync/O_SYNC, I/O schedulers and why NVMe should use none, io_uring — the shared-memory ring buffer architecture that eliminates per-I/O syscall overhead — readahead, writeback tuning, and benchmarking with fio), advanced networking (the kernel packet pipeline, nftables as the iptables successor, TCP tuning — buffer sizes and BBR congestion control, SO_REUSEPORT for per-CPU accept parallelism, IPVS for O(1) Kubernetes Service load balancing, traffic control with tc and netem, conntrack table exhaustion, and Unix domain sockets), eBPF (the verifier, maps, program types — kprobe/tracepoint/uprobe/XDP/TC/cgroup/LSM/sched_ext, XDP for 10M+ pps packet filtering, bpftrace one-liners, the BCC toolkit, CO-RE portability, libbpf, and the 2026 production landscape — Cilium, Tetragon, Falco, Pixie, Grafana Beyla, sched_ext), performance analysis (the USE method as a systematic checklist, the RED method for services, perf stat IPC for CPU-vs-memory-bound diagnosis, flame graphs — on-CPU and off-CPU, ftrace, advanced diagnostic patterns for latency, lock contention, and memory leaks), security hardening (SELinux vs AppArmor, seccomp-bpf syscall filtering, the audit framework, SSH hardening, PAM, the full systemd sandboxing reference, and kernel hardening features), advanced filesystems and storage (ext4 vs XFS vs Btrfs comparison, filesystem tuning, LVM — thin provisioning and snapshots, LUKS encryption, mdadm RAID, POSIX ACLs, overlayfs mechanics, mount propagation, and bind mounts), kernel tuning (the sysctl parameters that actually matter — somaxconn, tcp_tw_reuse, swappiness, BBR, inotify watches, conntrack_max — kernel command line parameters including isolcpus, nohz_full, and rcu_nocbs, and DKMS), and the boot process (the full sequence from power-on — UEFI vs BIOS, Secure Boot, GRUB2, initramfs as the driver-loading bridge to the real root, systemd's parallel startup orchestration, systemd-analyze for boot profiling, targets, and rescue/recovery).

[Open on GitHub](https://github.com/s4njee/larning/blob/main/ADVANCED_LINUX_STUDY_GUIDE.md)

### Advanced Node.js
This guide is a depth-first treatment of Node.js internals and high-performance patterns for engineers who already build production services with it. It starts with the runtime model (V8 + libuv + kernel async I/O, why single-threaded doesn't mean single-process, the thread pool), then goes deep on V8's JIT pipeline (Ignition → Maglev → TurboFan, hidden classes, inline caches, SMIs vs HeapNumbers, typed arrays, and the deoptimization traps that silently slow code by 100×). The event loop gets an honest treatment — the actual phase sequence (timers → poll → check), microtask queues, `process.nextTick` vs `queueMicrotask` vs `setImmediate`, and blocking-the-loop detection. It then covers async patterns and pitfalls (sequential vs concurrent `await`, `Promise.all`/`allSettled`, bounded concurrency, `AbortController`), streams and backpressure (`pipeline`, async iterators, `highWaterMark`), Worker Threads and clustering (offloading CPU work with `piscina`, `transferList` zero-copy, cluster vs Workers), memory management (V8's generational heap, the three-snapshot leak technique, common leak patterns, GC tuning), profiling (flame charts, Clinic.js, `monitorEventLoopDelay`), and the full ladder of performance levers — faster framework (Fastify), schema-compiled JSON, streaming, connection pooling, V8 shape discipline, caching, clustering, and native/Wasm. It closes with ten worked recipes: schema-compiled serialization, stream transforms, bounded fan-out, worker-pool image processing, database pooling, LRU with stale-while-revalidate, `setImmediate` chunking, zero-copy buffers, undici connection pooling, and graceful shutdown — plus a decision tree.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/ADVANCED_NODEJS_STUDY_GUIDE.md)

### Advanced Python
This guide is a depth-first treatment of the Python language and CPython runtime for engineers who already write Python professionally and want to write it *fast*. It starts with the runtime model (heap-allocated objects, reference counting, the GIL, bytecode, the specializing interpreter, and the experimental JIT and free-threaded builds), the data model (dunder protocols, the attribute lookup chain, container/numeric/callable/context-manager protocols, `__init_subclass__`), descriptors (`property`, method binding, `__slots__`, reusable validators), metaclasses and class machinery (`type`, `__prepare__`, `@dataclass`, the MRO), and iterators/generators/`itertools` for lazy constant-memory pipelines. It then covers the type system (generics, `Protocol`, `TypedDict`, strict mypy/pyright), modern packaging with `uv` and `ruff`, profiling and measurement (`cProfile`, `line_profiler`, `tracemalloc`, `timeit`), and the full ladder of performance levers — right data structure, move-the-loop-into-C, NumPy vectorization, caching, `__slots__`, string performance, concurrency, compiled extensions (Cython, Numba, PyO3/Rust, PyPy), and algorithm choice. It closes with ten worked, profiled high-performance recipes: set membership, batch inserts, large-file streaming, NumPy vectorization, Numba JIT, async HTTP fan-out, multiprocessing, `__slots__` memory, string building, and a decision tree for choosing the right lever.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/ADVANCED_PYTHON_STUDY_GUIDE.md)

### Advanced Rust
This guide is a depth-first, prose-first treatment of the Rust that senior work demands, written as the sequel to the Rust for Python Developers guide: it assumes ownership, borrowing, traits, and `Result`, and builds the advanced language in thirteen parts around one thesis — encode the invariant in the type system so violations don't compile. It covers the trait machinery (static vs. dynamic dispatch with the fat-pointer mental model and the hybrid idiom, dyn-compatibility, associated types vs. generic parameters, GATs taught through the lending iterator, coherence and the newtype/typestate patterns, const generics), lifetimes beyond the basics ('static as a bound, HRTBs, variance taught through the dangling-pointer exploit invariance prevents, and the self-reference wall that motivates Pin), memory layout (fat pointers, niche optimization, the completed smart-pointer table with Weak, OnceLock/LazyLock, and Cow), threads and data parallelism (Send/Sync as structural auto traits, scoped threads, channels vs. shared state, Rayon), atomics with the acquire/release handshake narrated on the canonical publish-a-flag example and an honest line on lock-free ambition, the async model (lazy state machines, poll/waker, Pin derived rather than asserted, reading future-is-not-Send errors structurally), Tokio in production as five disciplines (never block the executor, cancellation safety, structured concurrency, lock scoping, runtime observability with tokio-console), unsafe with the five powers, SAFETY-comment discipline, MaybeUninit, and Miri in CI, FFI through its three recurring hazards (ownership, unwinding, validity) and the -sys-crate convention, performance engineering (what zero-cost actually promises, allocation discipline, LTO/codegen-units/PGO/allocator swaps, criterion/flamegraph/dhat), the verification ladder (thiserror/anyhow, paused-time async tests, loom for atomics, proptest and cargo-fuzz), a right-sized macros part (macro_rules! hygiene, how derive macros work, when not to), and production service patterns (graceful shutdown, backpressure, tracing) — closing with four capstone projects tied back to the parts they exercise.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/ADVANCED_RUST_STUDY_GUIDE.md)

### AI Engineering: Agents & Tool Use
This guide is a depth-first treatment of building, orchestrating, and operating AI agents in production — the companion to the LLM Application Development guide, going deep on what changes when the LLM is in a loop. It starts with the mental model (the spectrum from single calls to multi-agent systems, Anthropic's workflows-vs-agents distinction, the five composable workflow patterns), then the agent loop itself (the minimal ~30-line loop in both Anthropic and OpenAI variants, ReAct, plan-and-execute, reflexion, and the full production checklist — cost tracking, loop detection, tool validation, human-in-the-loop). It goes deep on tool use (design principles, parallel calls, tool routing for large catalogs, result handling, provider comparison), structured outputs for agents (native constrained decoding, Instructor, structured internal state), guardrails (defense-in-depth with rule-based/ML/LLM layers, prompt injection defense, PII detection, action validation, Guardrails AI vs. NeMo Guardrails, the self-correction pattern), and cost control (the four levers — prompt caching, model routing and cascading, semantic caching, token budgeting — with an implementation roadmap). It then covers multi-agent orchestration (orchestrator-workers, sequential pipelines, fan-out/fan-in, debate, handoffs, shared state, production guardrails for multi-agent), MCP (the Model Context Protocol — architecture, three primitives, transport, writing servers and clients, security, 2026 status), A2A (Google's Agent-to-Agent protocol — Agent Cards, task lifecycle, how MCP and A2A compose), memory systems (the CoALA taxonomy of working/episodic/semantic/procedural memory, tools like Mem0, Zep, and Letta, the active reflection pattern), computer use and browser agents (the hybrid architecture, Anthropic computer use, frameworks), agent observability and evaluation (tracing, key metrics, platforms, trajectory evaluation, the production-to-eval pipeline), agent safety and alignment (risk taxonomy, least privilege, action classification, staged autonomy, red-teaming, regulatory frameworks), context engineering (the shift from prompt engineering, principles for information timing, tool descriptions, context hygiene), and the critical question of when to skip the framework (the case for raw SDKs, the case for frameworks, the hybrid approach, the Anthropic principle). It closes with a framework landscape comparison (LangGraph, OpenAI Agents SDK, CrewAI, AutoGen — with a selection matrix), four production recipes (customer support agent, research agent with memory, multi-agent code review pipeline, cost-optimized agent with model routing), common mistakes, and decision trees for agent, framework, and model selection.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/AI_AGENTS_STUDY_GUIDE.md)

### Ansible
This guide is a depth-first treatment of Ansible for engineers configuring real machines: the agentless model, inventories, modules, playbooks, roles, variables and Jinja2, control flow, templates, collections, testing, and production patterns — followed by an honest comparison against Terraform, Puppet, Salt, and cloud-init. It closes with concrete recipes: a full nginx role with every file, a rolling-deploy playbook with block-rescue rollback, a common-patterns cookbook (bootstrap, users, dotfiles, Vault secrets), and an end-to-end walkthrough of a production playbook repo.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/ANSIBLE_STUDY_GUIDE.md)

### API Design
This guide is a depth-first treatment of the craft of designing a single API well — the layer beneath the Enterprise API guide's gateways and governance — organized around the idea that an API is a promise you can't take back, made to consumers you'll never meet, who increasingly include LLM agents. It works through resource modeling (nouns vs. verbs honestly, opaque prefixed IDs, custom methods), HTTP semantics used properly (method guarantees, status codes that mean what they say, conditional requests), error design as a first-class discipline (RFC 9457 problem+json, errors an agent can act on), idempotency keys end to end, collection design (cursor pagination and why offset loses), versioning and evolution (the don't-break > additive > version hierarchy, Sunset headers, what "backwards compatible" precisely means), long-running operations and webhooks, an honest REST-vs-gRPC-vs-GraphQL-vs-events decision framework, spec-driven development with OpenAPI 3.1 (codegen, Spectral linting, contract testing), and a dedicated part on designing for agent consumers — where tool descriptions become prompts, enums beat free strings, and teams test their APIs by watching an LLM consume them cold. Closes with a worked booking-API walkthrough making every decision explicit.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/API_DESIGN_STUDY_GUIDE.md)

### Asyncio & aiohttp
This guide is a depth-first treatment of Python's `asyncio` for engineers making I/O-bound code fast and keeping it correct, with heavy weight on `aiohttp` and practical performance. It starts with the mental model (why async eliminates idle waiting, I/O-bound vs CPU-bound, the event loop, coroutines vs tasks vs futures), then the concurrency toolkit (`gather`, `TaskGroup`, `as_completed`, `wait`), cancellation/timeouts/exception groups, coordination and backpressure (`Semaphore`, `Queue`, `Lock`, `Event`), and the cardinal rule of never blocking the loop (detection, `to_thread`, executors, `aiofiles`). It then goes deep on `aiohttp` — the client (session reuse, connection pooling, timeouts, streaming, bounded fan-out, retries) and the server (application lifecycle, middleware, streaming, WebSockets, multi-worker deployment) — and on talking to data (`asyncpg` vs `aiopg`, pools, the accidental-serialization trap, `aiofiles`). It closes with a performance chapter (measuring, `uvloop`, killing serialization, batching with `COPY`, pool sizing, when async stops helping), a production/debugging field guide, and five performance recipes including concurrent download-to-disk and a streaming file-to-Postgres ETL pipeline.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/ASYNCIO_STUDY_GUIDE.md)

### Authentication & Authorization (Identity)
This guide is a depth-first treatment of identity in modern distributed systems, answering the applied "how do I do auth in my app" question. It bridges the gap between cryptographic primitives and cluster-level access. It covers the fundamentals (stateful sessions vs. stateless JWTs, the LocalStorage vs. HttpOnly cookie debate for XSS/CSRF), OAuth 2.0 and OIDC (the Authorization Code flow with PKCE, the distinction between Access Tokens and ID Tokens), token lifecycles (refresh-token rotation, compromise detection, revocation strategies like blocklists and Bloom filters), enterprise SSO (SAML 2.0 vs. OIDC, SCIM for directory sync), and authorization models (RBAC, ABAC, ReBAC via Google Zanzibar/SpiceDB, and multi-tenant authz isolation). It then covers application architecture patterns (API Gateways, the Phantom Token pattern, the WebSockets auth-header problem) and passkeys/WebAuthn. It closes with common pitfalls like the `alg=none` vulnerability, confused deputy/audience checks, and token exfiltration via `Referer`.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/AUTH_STUDY_GUIDE.md)

### AWS Fundamentals
This guide builds AWS fluency from scratch for engineers who can build and deploy software but haven't worked seriously inside AWS — the prerequisite the repo's Azure-for-AWS and GCP-for-AWS architect guides have always assumed. Its organizing idea is that AWS is a programmable permission system (IAM) wrapped around a software-defined network (VPC) wrapped around two hundred services, with every interaction an authenticated, authorized, logged API call — so it front-loads the two universal layers before any compute: IAM (principals, policies, the deny-overrides-allow evaluation logic, and why AssumeRole with temporary credentials is the single most important verb while long-lived access keys are the classic breach), and VPC (public-vs-private as a routing property, security groups as stateful intent-expressing firewalls, NAT gateways and the endpoint economics that tame them). It then works through compute as a control-vs-burden spectrum (EC2/ASG/ALB, Lambda's honest trade-offs, the ECS-vs-EKS decision), S3 and the storage tiers, databases (Multi-AZ vs read replicas precisely distinguished, DynamoDB partition-key design as destiny), messaging (SQS visibility timeouts and idempotent consumers, SNS/EventBridge fan-out, Step Functions as managed sagas, Kinesis vs MSK), operations (IaC, CloudWatch/CloudTrail, cost traps as a rogues' gallery, Organizations and SCPs), and the 2026 AI surface (Bedrock vs SageMaker vs self-hosted GPU economics). A capstone traces one request through a full three-tier architecture with the IAM role and security group named at every hop, on the thesis that the architecture diagram and the bill are the same diagram.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/AWS_FUNDAMENTALS_STUDY_GUIDE.md)

### Azure for AWS Solutions Architect
This guide is a prose-first translation of AWS architecture instincts into Azure for engineers who already think in AWS primitives, built around the genuine conceptual differences rather than a table of name mappings. After a translation map and CLI/Bicep quickstart, each of fifteen sections opens on the AWS mental model you hold and then develops what actually differs: the Azure hierarchy (tenant → management group → subscription → resource group) and the resource group as a hard lifecycle container with no AWS equivalent; the identity-vs-authorization split between Entra ID and Azure RBAC with managed identities as secret-free workload identity and Azure Policy that audits and remediates rather than only denying; networking's one-to-many edge (NSG replacing SG+NACL, Route 53 splitting into DNS/Traffic Manager/Front Door, the L7-vs-L4 load-balancer split); the managed-tier ladder that recurs across compute, databases (the SQL Database → Managed Instance → VM rungs, no Aurora equivalent), and containers (Container Apps before AKS); storage redundancy as an explicit LRS/ZRS/GRS/GZRS choice; Cosmos DB's five consistency levels and partition-key sensitivity; the message-vs-event distinction sorting Service Bus / Event Grid / Event Hubs; the Step Functions split into Durable Functions vs Logic Apps; observability fragmenting into Monitor/App Insights/Log Analytics with KQL as the load-bearing skill; Key Vault and Defender for Cloud as rare consolidations; landing zones as a full platform baseline; and the Backup-vs-Site-Recovery distinction. Every section keeps its quick-reference mapping table, hands-on az/Bicep code, official docs, and practice prompts — now surrounded by the flowing exposition that teaches the why.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/AZURE_FOR_AWS_SOLUTIONS_ARCHITECT.md)

### GCP for AWS Solutions Architect
This guide is a prose-first translation of AWS architecture instincts into Google Cloud for engineers who already think in AWS primitives, built around the genuine conceptual differences rather than a table of name mappings. Each of fifteen sections opens on the AWS mental model you hold and develops what actually differs: GCP's strict Organization → Folder → Project hierarchy and the project as a lightweight unit you create many of (inverting AWS's account-rationing); the resource-centric IAM model where policies attach to resources not principals, with service accounts as both principal and resource and Workload Identity Federation for secret-free access; the global VPC and global anycast load balancer that make cross-region plumbing largely unnecessary; the managed-tier ladders across compute (custom machine types, OS Login, regional MIGs), databases (Cloud SQL → AlloyDB → the no-AWS-equivalent globally-consistent Spanner with its TrueTime atomic clocks), and containers (Cloud Run before GKE Autopilot); GCS storage classes that share one millisecond-latency API so cold data is a pricing not an architecture decision; the collapses where one GCP service covers several AWS ones (Pub/Sub as SQS+SNS, BigQuery as Redshift+Athena with multi-cloud Omni/BigLake); the observability suite with its Log Router/BigQuery-sink idiom; and the distinctive security controls with no clean AWS equivalent — Identity-Aware Proxy for VPN-less zero-trust access and VPC Service Controls for exfiltration perimeters around authenticated identities. Every section keeps its quick-reference mapping table, hands-on gcloud/Terraform code, official docs, and practice prompts — now surrounded by the exposition that teaches the why.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/GCP_FOR_AWS_SOLUTIONS_ARCHITECT.md)

### Blender
This guide is a concept-first tour of Blender, the free, open-source 3D creation suite — it builds the mental model behind each major system rather than documenting every button. It covers the pipeline mindset and data model (data-blocks, the user-count system, the `.blend` database), the editor and mode paradigm, navigation, modeling (topology, the modifier stack, non-destructive workflows), materials and PBR shading, UV mapping, lighting, cameras and the EEVEE-vs-Cycles render decision, and sculpting. It goes deep on **rigging** — armatures and bone orientation/roll, the deform-vs-control-bone model, weight painting and mirroring, FK/IK and IK/FK switching, pole targets, Spline IK, Bendy Bones, bone constraints, custom control shapes and bone collections, Rigify, corrective shape keys, and the apply-scale-first gotchas — and on **animation** — keyframes and keying sets, the Graph Editor and F-curve handle types, cyclic motion, the twelve principles, the blocking → splining → polish workflow, pose tools and motion paths, the NLA editor, drivers, shape keys, and a worked walk-cycle example. It closes with Geometry Nodes, particles and physics, compositing, video editing, add-ons and Python scripting, production file management, and a mastery checklist.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/BLENDER_STUDY_GUIDE.md)

### Blender Animation
This is a depth-first sub-guide of the Blender guide, aimed at a mid-level developer who would rather understand the interpolation math once than memorize which handle type looks right. Its organizing claim is that an animated pose is a pure function of time, and the whole animation system is the machinery for defining, composing, and evaluating that function — so it works bottom-up through the pipeline. It starts with the dependency graph and the original-vs-evaluated data split (why a script reading `obj.location` gets the wrong answer, why animation always loses to constraints, why RNA data paths make bone names an API), then the math of F-Curves as piecewise cubic Béziers — the `x(t)` inversion that forces handle clamping, handle types as tangent constraints, Penner's easing equations, and F-Curve modifiers as a function stack. Rotation gets its own part: gimbal lock, Shoemake's quaternions, and the fact that Blender's four quaternion channels are independent scalar curves that do *not* slerp, which is where the long-way-round spin and the double-cover sign flip come from. It then covers Actions as reference-counted data-blocks and the 4.4+ slot/layer/strip/channelbag model with version-tolerant Python, the NLA as a blend stack (and why Combine is the only mode that composes rotations correctly), drivers as dependency-tracked spreadsheet cells with the simple-expression fast path and the stale-value danger of dependency cycles, and deformation — the linear blend skinning equation, why candy-wrapping is inherent rather than a weighting mistake, dual quaternions, and pose space deformation behind corrective shapes. The craft half covers spacing versus timing, arcs and motion paths, moving holds, the blocking → splining → polish passes, and computing ballistic frame counts from `h = ½gt²` instead of guessing them. It closes with a jump authored twice (once in the UI, once in `bpy`, compared numerically), the `bpy` animation API with baking and mocap retargeting, glTF/FBX export realities and what real-time engines actually consume, a debugging field guide of a dozen concrete failure modes, and an annotated bibliography splitting the primary literature (Lasseter 1987, Shoemake 1985, Kochanek–Bartels, Lewis et al.'s pose space deformation, Kavan's dual quaternions, Gleicher's retargeting, FABRIK, the glTF spec, the Blender source) from the third-party literature (Williams, Thomas & Johnston, Parent, Osipa, Gregory, Blender Studio, 11 Second Club).

[Open on GitHub](https://github.com/s4njee/larning/blob/main/BLENDER_ANIMATION_STUDY_GUIDE.md)

### Caddy
This guide is an exhaustive, opinionated treatment of the Caddy web server organized by its 'HTTPS should be the default' thesis — now with that signature feature and the reverse proxy developed conceptually rather than presented as config to copy. Automatic HTTPS is framed by the problem it solves: the certificate-authority trust model, the manual annual chore of obtaining CA-signed certificates (and the forgotten-renewal outage it caused), and how the ACME protocol and Let's Encrypt automated domain-ownership proof via challenges — so that Caddy building a full ACME client into the server and turning it on by default makes 'HTTPS should be the default' an architecture, not a slogan, where a certificate is a thing the server acquires and maintains like a listening socket. The reverse proxy is developed as the seam between the public internet and a simple private backend — what forward-vs-reverse means, and why TLS termination, load balancing with health checks and retries, and the X-Forwarded-* headers all follow from the proxy being the single public entry point. Around those, the guide covers installation, the Caddyfile language in full, static serving, request handling, authentication, the JSON config and admin API, logging and observability, production operations, an honest comparison to Nginx/Apache/Traefik/HAProxy/Envoy, and end-to-end recipes — every config block grounded in the role it serves.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/CADDY_STUDY_GUIDE.md)

### CB8 on iOS
This guide is a practical case study in porting an Electron + web-GUI desktop app to iOS, using [CB8](https://github.com/s4njee/CB8) — a comic and book reader with an embedded Fastify server, React/shadcn renderer, SQLite index, and node-7z archive handling — as the real codebase being ported. It starts by reading CB8's architecture to see what survives the port (the HTTP API, the tested pure logic in `src/shared/`, the SQLite schema) and what categorically cannot (Electron, Node, spawned 7-Zip executables), then weighs the four porting strategies honestly — wrapping the web UI in WKWebView/Capacitor, React Native, on-device Node runtimes, and native SwiftUI — and builds the recommended two-phase native answer. Phase 1 is a Swift client for CB8's existing server API: the full endpoint table transcribed from the route handlers, Codable models mirroring `WebComicRecord`, better-auth session cookies handled for free by `URLSession`, a paginated `LazyVGrid` library, and a native comic reader with `UIScrollView` zoom, prefetch windows, server-side `?width=` resizing, and interoperable progress writes. Phase 2 goes standalone: document-picker imports with security-scoped bookmarks, a GRDB port of CB8's schema (minus the server's auth tables), ImageIO thumbnail downsampling, ZIPFoundation CBZ access, the CBR/unrar licensing decision, and behavior-identical Swift ports of `naturalSort`, `imageFilter`, and the LRU cache — verified by transcribing CB8's vitest suites into Swift Testing. It closes with offline downloads and last-write-wins progress sync, App Review preparation specific to self-hosted-client apps (guideline 4.2, ATS exceptions, local network privacy, demo servers), and an appendix giving the WKWebView/Capacitor wrapper recipe as a stopgap.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/CB8_IOS_STUDY_GUIDE.md)

### CB8 on Android
The Android sibling of the CB8 on iOS guide: porting the same Electron + web-GUI comic reader to Android, with a deliberately parallel part structure so the two read as a side-by-side of how the platforms change the answers. It opens with the Android delta — processes can be spawned (the node-7z verdict softens), JIT is legal (on-device Node gets an honest hearing instead of a flat no), the Storage Access Framework replaces security-scoped bookmarks, WorkManager makes background scans and downloads first-class, and Play review is lighter while the store itself is optional — then re-runs the strategy decision and builds the same two-phase native answer in Kotlin and Jetpack Compose. Phase 1 is a client for CB8's existing server API: kotlinx.serialization mirrors of the wire types, Retrofit/OkHttp with the persistent cookie jar Android doesn't give you for free, scoped cleartext policy for LAN servers, a Paging 3 library grid (a natural fit for the offset/limit API), Coil thumbnails honoring the cache-buster contract, and a HorizontalPager + Telephoto reader with prefetch, RTL via reverseLayout, and interoperable progress writes. Phase 2 goes standalone: SAF tree grants with cursor-based scanning (and why DocumentFile recursion is too slow), a Room port of CB8's schema, WorkManager scan workers, java.util.zip CBZ access, the CBR landscape (junrar's RAR5 gap, libarchive via NDK, and the bundled-7z-in-jniLibs trick that legally re-homes node-7z's actual design), and Kotlin ports of the shared utilities verified by JVM-only test transcriptions with kotest property testing standing in for fast-check. It closes with WorkManager-backed offline sync, a distribution strategy chapter (Play, F-Droid build flavors forced by the unrar license, and direct APK via GitHub Releases matching CB8's desktop channel), an appendix that actually walks the on-device Node experiment to show precisely where it breaks down, and the Kotlin Multiplatform consolidation path if both mobile ports become real.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/CB8_ANDROID_STUDY_GUIDE.md)

### Cloudflare
This guide is a mechanism-first treatment of Cloudflare for engineers who already think in AWS primitives, built on the two-halves framing: the network/security half you put in front of infrastructure you already run, and the developer platform half where the application itself lives at the edge. It opens by deriving the platform's behavior from two architectural decisions — anycast (why there are no regions, why DDoS absorption is structural, why every PoP runs the full stack) and the single proxy pipeline (the request-lifecycle order of operations that makes debugging a question of which stage decided what) — then builds out both halves with the mechanism under each product: DNS and the orange-cloud toggle with origin-IP-leakage hygiene, the two-hop TLS model and the Flexible-SSL trap, caching as the cache-key decision chain (and the cache-everything-served-user-A's-page-to-user-B sin), the WAF's one-language/one-pipeline advantage with Bot Management, Turnstile, and API Shield, rate limiting as three design decisions (characteristic, counting expression, threshold), Workers as the V8-isolate model versus Lambda's Firecracker (CPU-time billing, bindings as capabilities, Smart Placement), R2's zero-egress economics modeled honestly, D1 as a fleet of per-tenant SQLites, KV's ~60-second consistency window derived from its centralized-store-plus-edge-cache architecture, Durable Objects as the single-threaded coordination primitive (hibernation, alarms, the global-singleton anti-pattern), the glue tier (Queues, Hyperdrive, Vectorize, Workers AI, AI Gateway), Zero Trust and Tunnels as the outbound-only inversion, traffic engineering (Load Balancing, Argo, Spectrum), media, and an observability/automation chapter (Logpush, Analytics Engine, Terraform, scoped tokens). It closes with three adoption patterns, a pitfalls catalog where every entry references the section that explains it, pricing read structurally, and decision trees — with practice exercises and documentation links throughout.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/CLOUDFLARE_STUDY_GUIDE.md)

### C++26 & Modern C++
This guide is a depth-first treatment of the C++26 standard and the paradigms that define modern C++ in 2026 — for engineers who know C++ (any vintage) and want to understand what changed, what to adopt, and what to abandon. It starts with the C++ timeline, then covers the modern paradigm shift: RAII, value semantics, Rule of Zero, move semantics, compile-time programming, concepts, ranges, sum types via `std::optional`/`variant`/`expected`, and deducing `this`. It goes deep on the three headline C++26 features: static reflection, contracts, and `std::execution`, while also covering the everyday concurrency toolkit (`std::jthread`, `stop_token`, C++20 synchronization primitives, and coroutines), C++26 language and library additions, and the honest state of C++ safety compared with Rust. The expanded practical sections are intentionally opinionated: project layout, house style, ownership/lifetime policy, API design, error handling, structured concurrency, testing and quality gates, performance defaults, banned/blessed idioms, code review, dependency management, ABI reality, and a staged modernization strategy for enterprise codebases. It closes with an old-way-vs-new-way reference table, and cross-links to the Rust, Advanced Go, Python Concurrency, ESP32, and GitHub Actions guides.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/CPP26_STUDY_GUIDE.md)

### Compiler and Language Internals
This guide is a textbook-level treatment of what compilers, interpreters, and language runtimes actually do, for engineers who want every "why is this slow / why was this rejected / why did -O2 delete my check" mystery to become tractable. A miniature language is built incrementally in plain Python — lexer, Pratt parser, resolver, type checker, bytecode compiler, and stack VM — so each chapter's theory attaches to forty lines of running code, and every claim is verifiable with the tools the guide teaches (Compiler Explorer, Python's tokenize/ast/symtable/dis, rustc --emit, node --trace-deopt). It covers the pipeline end to end: lexing (maximal munch, Python's synthesized INDENT/DEDENT tokens), parsing (recursive descent and precedence climbing worked in full, why hand-rolled parsers beat generators industrially, error recovery as the IDE-era requirement, CPython's PEG switch), name resolution and closures (cell variables, two-pass resolution, hoisting as a resolution rule), type systems in two chapters (nominal vs. structural with TypeScript's deliberate unsoundness catalogued and defended, variance derived from first principles, Hindley-Milner unification worked by hand, Rust's borrow checker located as an affine type system running dataflow on MIR), IRs and SSA (phi nodes, dominance, readable LLVM IR), optimization as theorems licensed by language axioms (UB as license, inlining as the enabling pass, the alias-analysis gatekeeper, LTO/PGO), code generation (graph-coloring vs. linear-scan register allocation, calling conventions as the ABI treaty), linking and loading (relocations, ODR, PLT/GOT lazy binding, the template tax), bytecode VMs (the lab VM mirrors CPython's, dispatch costs, the 3.11 specializing adaptive interpreter), JIT compilation (V8's four tiers, hidden classes and inline caches decoding the object-shape folklore, speculation and deoptimization, tracing JITs and CPython's copy-and-patch), and garbage collection (the generational hypothesis, write barriers, tri-color concurrent marking, GC pressure as a program-design variable). It closes by touring the real systems — CPython, tsc as a checker wearing a compiler costume, rustc's tower of IRs and query system, LLVM versus Go as compiler-architecture-as-product-strategy — with exercises closing every chapter.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/COMPILER_INTERNALS_STUDY_GUIDE.md)

### Cryptography Fundamentals
This guide covers applied cryptography for engineers: the primitives (hashing, symmetric, asymmetric, signatures, key exchange), the protocols (TLS, OIDC, JWT), the engineering concerns (key management, side channels, constant-time code), and the applied recipes you actually ship (auth in Node.js and Go, webhook signing, envelope encryption with KMS, file encryption with age). The goal is to be fluent enough to read a spec, choose the right library, and recognize when something is dangerously wrong.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/CRYPTO_FUNDAMENTALS.md)

### Data Engineering
This guide covers what data engineers actually build: storage formats, warehouses and lakehouses, ingestion (CDC, Kafka, SaaS), transformation with dbt, orchestration with Airflow/Dagster/Prefect, distributed compute with Spark, streaming, modeling, quality, and governance. It closes with concrete recipes for a real dbt project layout, Airflow DAG patterns, warehouse cost optimization, and a full walkthrough of a production pipeline from source systems to BI.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/DATA_ENGINEERING_STUDY_GUIDE.md)

### Database Internals: PostgreSQL and SQLite
This guide is a textbook-level treatment of what happens beneath SQL, taught through the two most instructive engines to study side by side: PostgreSQL and SQLite are both single-node and B-tree-based yet disagree about nearly everything else — client-server versus in-process, heap tables with secondary indexes versus clustered B-trees, row-version MVCC with vacuum versus whole-database snapshots with a single writer — so every divergence becomes a lesson in the design space. In the style of the Distributed Algorithms guide (precise mechanisms, byte-level layouts, exercises closing every chapter, most of them runnable with pageinspect, pg_waldump, EXPLAIN, dbstat, and sqlite3_analyzer), it covers: slotted pages and on-disk layout (PostgreSQL's 23-byte tuple headers as the price of MVCC, TOAST; SQLite's varints, serial types, and overflow chains), B-trees from the fanout arithmetic up through PostgreSQL's Lehman–Yao right-links and suffix truncation/deduplication and SQLite's table-is-the-index clustering and WITHOUT ROWID, the buffer pool (clock sweep, ring buffers, double buffering; SQLite's pager and the mmap trade), logging and recovery (redo-only WAL with full-page images, checkpoints, group commit, and the fsyncgate cautionary tale; SQLite's rollback journal and WAL modes with the atomic-commit failure-window analysis), MVCC and isolation (visibility rules, hint bits, vacuum and wraparound freezing, HOT updates, write skew, and SSI; versus SQLite's serializable-by-mutual-exclusion model), locking (latches vs. row locks vs. table locks, the DDL pile-up, SQLite's lock ladder and SQLITE_BUSY), query planning (statistics and selectivity, the independence-assumption trap and error compounding through joins, System R dynamic programming and GEQO versus SQLite's NGQP), execution (the Volcano iterator model, join algorithms, bitmap scans, JIT — versus the VDBE bytecode machine and prepared statements), the index menagerie (GIN's posting trees and pending list, GiST, BRIN, partial/expression indexes, pgvector's approximate indexes; FTS5 and R*Tree on the xBestIndex virtual-table hook) plus an honest B-tree-versus-LSM amplification analysis (leveled vs. tiered compaction, WiscKey) and the RUM conjecture, a full chapter on schema changes and corruption (catalog-only vs. rewrite ALTERs, the NOT VALID/VALIDATE two-step, SQLite's 12-step recipe and schema cookie, amcheck and integrity_check, and how databases actually get corrupted), and from-memory walkthroughs tracing a SELECT and an UPDATE through every layer of each system. It closes with a curated path into CMU 15-445, the PostgreSQL source READMEs, and SQLite's source as the most literate storage codebase to read first.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/DATABASE_INTERNALS_STUDY_GUIDE.md)

### Distributed Algorithms
This guide is the textbook-level companion to the Distributed Systems guide: where that guide teaches what the CAP theorem says and which systems embody which trade-offs, this one teaches why CAP is true, why consensus is impossible in the asynchronous model, and what invariant makes Paxos safe — with formal models, exact theorem statements, proofs an engineer can follow and reconstruct, transcribable pseudocode, and exercises closing every chapter. It builds the field in order: models of computation (the synchrony spectrum, crash vs. crash-recovery vs. Byzantine failures, safety vs. liveness as the lens for reading any specification), logical time (happens-before, the Lamport clock theorem and its false converse, the vector clock iff-theorem and version vectors, HLC and TrueTime), the broadcast ladder (reliable, FIFO, causal with its hold-back algorithm, and the theorem that total order broadcast is equivalent to consensus — the price list for the whole field), Chandy–Lamport snapshots (consistent cuts, the marker proof, and Flink checkpoints as the algorithm in production), and the FLP impossibility proof via bivalent configurations together with its loopholes — partial synchrony, randomization, and the failure-detector hierarchy down to Ω. The consensus core derives single-decree Paxos rule by rule with the P2c safety proof, then walks Raft at the message level (election, log matching, the current-term commit rule and Figure 8, the Leader Completeness proof, membership change, and the lease-read timing caveat). It then covers replication theory (linearizability defined precisely, why R+W>N is not sufficient, the ABD algorithm with its reads-must-write cost, and the Gilbert–Lynch CAP proof in full), atomic commitment (2PC and the blocking theorem's indistinguishability argument, why 3PC fails under partition, and the modern resolution of 2PC over Paxos as in Spanner), CRDTs (the join-semilattice convergence theorem, G-Counter/PN-Counter/LWW/OR-Set worked by hand, op-based vs. state-based, and the CALM principle marking exactly where coordination must be paid for), gossip (push/pull epidemic math, Merkle-tree anti-entropy, SWIM failure detection), and Byzantine fault tolerance (the 3f+1 bound, PBFT's two-round structure explained by what survives a view change, HotStuff, and Nakamoto consensus located on the same map). It closes by mapping every algorithm onto the systems that run it and teaching how to read a Jepsen report — or your own incident — as a chapter violation.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/DISTRIBUTED_ALGORITHMS_STUDY_GUIDE.md)

### Distributed Systems
This guide is a concept-first but relentlessly OSS-grounded treatment of distributed systems for engineers who build and operate them. It builds bottom-up: first the physics (partial failure, the unreliable network, why clocks lie, logical and vector clocks, gossip, and the FLP and Two Generals impossibility results), then the core techniques layered on top — replication and the consistency spectrum (single-leader/multi-leader/leaderless, quorums, CAP done honestly and PACELC, CRDTs), consensus (the replicated-state-machine model, Paxos intuition, Raft in depth, and why clusters run 3 or 5 nodes), and partitioning (range vs. hash, consistent hashing with virtual nodes, hotspots, rebalancing, request routing). It then assembles those primitives into real systems: coordination services and the distributed-locks-are-dangerous cautionary tale (fencing tokens, the Redlock critique), transactions and ordering (the dual-write problem, idempotency, why 2PC is avoided, sagas, the outbox pattern, TrueTime/HLC), and messaging and streaming (queue vs. log, Kafka as a worked example, delivery semantics, backpressure, dead-letter queues) — before a production chapter on operating them (distributed tracing, the failure taxonomy of cascades, retry storms, and metastable failure, and the survival toolkit of timeouts, backoff with jitter, circuit breakers, bulkheads, and load shedding). Every concept is anchored in open-source systems you can run — Kafka, Cassandra, etcd, ZooKeeper, Consul, Redis Cluster, CockroachDB, Elasticsearch, Kubernetes — and it closes with a field guide that maps the concepts onto those systems plus end-to-end walkthroughs of a write through Kafka, a write-and-read in Cassandra, and Kubernetes as a distributed system in its own right.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/DISTRIBUTED_SYSTEMS_STUDY_GUIDE.md)

### Django
This guide covers Django as a productive web framework with a strong opinionated core. It focuses on the parts that matter most in day-to-day development: models, views, templates, forms, admin, auth, and how those pieces fit together in a maintainable app.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/DJANGO_STUDY_GUIDE.md)

### Docker
This guide teaches Docker from the under-the-hood reality up — containers as processes isolated by namespaces and constrained by cgroups, not lightweight VMs — through a comprehensive scope (images and layers, the Dockerfile in depth, multi-stage builds, BuildKit, volumes and mounts, networking, Compose, health checks, resource limits, security, the containerd/runc/OCI runtime stack, registries, logging, and production patterns and mistakes), now with the deepest middle-chapter mechanism developed expository rather than terse. Images and layers are explained through the union filesystem (overlayfs) and copy-on-write that make them work: read-only layers stacked and merged into one filesystem, a thin writable layer per container, files copied up only on write — which is *why* a container starts in milliseconds despite a gigabyte image, why ten containers share one on-disk copy of their layers, and why the second image on a shared base pulls fast, the entire cheap-to-start-store-and-run value proposition sitting in that one design. The container-as-namespaced-process foundation, the layer-caching and instruction-order discipline, the security hardening, and the runtime-stack chapter round it out, with the dedicated Docker/Kubernetes Networking guide cross-linked for the full data-path theory.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/DOCKER_STUDY_GUIDE.md)

### Docker and Kubernetes Networking
This guide teaches container networking through the Docker-pattern-to-Kubernetes-equivalent mapping, now grounded in the actual networking stack underneath. Its foundation develops, as flowing prose, the four Linux primitives every flag and object reduces to — network namespaces (why two containers can both bind port 80), veth pairs (the patch cable out of the namespace), the bridge (the virtual switch joining them), and iptables NAT (what `-p 8080:80` actually installs) — then shows how Docker NATs aggressively at every host boundary while Kubernetes deliberately builds a flat, NAT-free Pod network so a Pod's IP is the address everyone uses. It traces the full Kubernetes data path concretely: the network-model rule that every Pod is directly routable, the CNI plugin that does the veth plumbing and cross-node routing (and enforces NetworkPolicy, or doesn't), the Service as a fiction maintained by kube-proxy's iptables/IPVS/eBPF rules rewriting a ClusterIP to a random healthy Pod, CoreDNS turning Service names into those ClusterIPs, and Ingress/Gateway as the cluster's HTTP front door. On that foundation it maps eight common Docker patterns (publish a port, private app-and-database network, reverse proxy, multi-service stack, tightly-coupled helper, host network, public/private tiers) to their Kubernetes equivalents with runnable Docker and YAML side by side, a troubleshooting phase with real incident walkthroughs and the kubectl/iptables checks you use constantly, and an advanced phase (headless Services and StatefulSets, Gateway API, service mesh, default-deny zero-trust, topology-aware routing, Multus multi-interface Pods, multi-cluster service networking).

[Open on GitHub](https://github.com/s4njee/larning/blob/main/k8s/DOCKER_KUBERNETES_NETWORKING_STUDY_GUIDE.md)

### Dotnet for Python Developers
This guide is for Python developers who want to become comfortable in the .NET ecosystem without having to relearn everything at once. It focuses on translating familiar Python mental models into C#, tooling, and common application structure.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/DOTNET_FOR_PYTHON_DEVS.md)

### Durable Execution & Workflow Engines
This guide covers durable execution — the technique behind Temporal, Restate, Inngest, DBOS, Step Functions, and Azure Durable Functions — on the thesis that it is event sourcing applied to the call stack: record every step a program takes as an event history, and recover from any crash by replaying the code with recorded results injected until it catches up to where it died. It opens with the problem (multi-step processes whose coordination state traditionally gets smeared across status columns, queues, and cron sweepers), develops the replay mechanism precisely, then works through the programming model (the determinism contract and what it bans, activities as the side-effect escape hatch, the signals/timers/queries waiting toolkit, sagas as a readable try/except), the exact failure semantics (exactly-once orchestration around at-least-once side effects — idempotency survives intact), and versioning as the ongoing tax on code that must replay last month's decisions (patching, worker pinning, thin-workflow discipline). An engines part maps the landscape (Temporal's lineage from SWF and Cadence, the code-vs-DSL split with Step Functions, Restate's log-based runtime, DBOS as a Postgres library, plus the Airflow-is-not-this disambiguation), a dedicated part covers AI agents as the newest customer (LLM calls as nondeterministic activities, approvals as signals racing timers, the history as the agent trace), and production guidance covers continue-as-new, time-skipping tests, replay testing in CI, and when a queue is all you needed. A walkthrough runs one order through a crash, a business failure with compensation, and a live deploy with a patch marker — reading the event history at each step.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/DURABLE_EXECUTION_STUDY_GUIDE.md)

### eBPF
This guide covers the entire eBPF landscape — the verifier, maps, program types and hook points, bpftrace and BCC, CO-RE, networking (XDP/TC, Cilium), security (Tetragon, Falco), observability, and sched_ext — with the two deepest mechanisms now developed as proper prose rather than stated and moved past. The verifier is explained as what it actually is: an abstract-interpretation proof checker that symbolically executes the program along every possible path, tracking an abstraction of every register (scalar or pointer, into what, with what bounds, possibly null), forking its knowledge at each branch — which is why bounds checks make accesses provable, why branch-heavy programs blow the verification budget, and what 'fighting the verifier' really means (making a safety property provable by this path explorer). CO-RE is developed as the two-part relocation mechanism — BTF embedding the kernel's exact struct layouts, the compiler emitting field-offset relocations the loader patches against the running kernel's BTF — that took eBPF from recompile-everywhere to one binary that runs across kernel versions whose layouts differ. Around those, the guide builds from a clear motivating part 1 (why run sandboxed JIT-compiled programs in the kernel at all) through the architecture pipeline (bpf syscall → verifier → JIT → hook points), the tracing toolkits, writing programs with libbpf/cilium-ebpf/aya, and the 2026 production ecosystem where eBPF is the substrate beneath Kubernetes networking, runtime security, and zero-instrumentation observability.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/EBPF_STUDY_GUIDE.md)

### Electron
This guide teaches desktop GUI development through Electron, written specifically for web and Linux/backend engineers who have never built a native app. Its framing: Electron is a web front-end (Chromium) wired to a Node.js back-end, so ~80% of it is skills you already have — the UI is web (use Vue), the privileged layer is Node, and the boundary between them is a client-server API — and the guide spends its energy on the genuinely new 20%. It covers the multi-process architecture (main vs renderer vs preload, mapped to a one-machine client-server system), IPC as a typed API boundary, the renderer as a web app you fully control, native OS integration (menus, tray, dialogs, shortcuts, deep links, and the per-OS conventions), local-first data (`app.getPath`, `electron-store`, SQLite via `better-sqlite3`, the native-module rebuild gotcha), and the security model (untrusted renderer with OS access — context isolation, sandbox, validated IPC, CSP, navigation control, and owning Chromium's CVEs). It then tackles the parts that shock every web developer — packaging and code signing per OS (NSIS/Authenticode, macOS notarization, and Linux AppImage/deb/Flatpak), auto-updates as the desktop "deploy," and performance (the honest memory/size cost, never blocking the main process). It closes with an opinionated comparison to Tauri, Qt, native, and PWAs — when to use Electron and when not to — and an end-to-end build walkthrough.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/ELECTRON_STUDY_GUIDE.md)

### Enterprise-Grade APIs
This guide is a depth-first treatment of designing, building, and operating APIs that other teams — and other companies — can bet their systems on. Its framing: "enterprise-grade" is a posture, not a feature list — the API is a contract with consumers you cannot redeploy, judged by how it behaves under partial failure, concurrent access, malicious input, and version skew, with Hyrum's Law as the operating condition. It covers resource and error design (HTTP method semantics as a contract, opaque prefixed IDs, integer money, RFC 9457 problem details with stable machine-matchable codes), then gives idempotency the load-bearing treatment it deserves — the precise definition (f(f(x)) = f(x), about system effect not response, including concurrent duplicates), why retries make it mandatory (exactly-once delivery is impossible; exactly-once effect is at-least-once plus dedup), the Idempotency-Key pattern implemented properly (atomic key reservation, body-hash conflict detection, in-flight races, replaying failures selectively, propagating keys downstream), and natural idempotency by design as the preferred alternative. From there: versioning and compatibility (additive evolution enforced by CI-gated contract diffing, URI vs date-pinned versioning, deprecation as a protocol with Sunset headers), cursor pagination (why offset is a correctness bug, stable unique sort keys, opaque cursors), concurrency control (ETags/If-Match against lost updates, consistency promises as documentation), authentication and authorization (prefixed rotatable keys, JWT validation sins, BOLA and the authorization chokepoint, multi-tenancy), rate limiting vs quotas vs load shedding (token bucket, 429 with Retry-After as a teaching device), resilience (descending timeouts, full-jitter backoff, retry budgets, circuit breakers, and why the SDK you ship decides consumer behavior), long-running operations and webhooks (202 + operation resources, HMAC signing, at-least-once delivery making consumer idempotency a documented requirement), observability (request IDs as the spine, RED metrics, SLOs, audit logs), contract testing (spec-as-artifact, breaking-change gates, the adversarial suite that replays idempotency keys concurrently and attempts cross-tenant access), and the gateway-vs-service split — closing with a design-review checklist traceable back to every part.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/ENTERPRISE_API_STUDY_GUIDE.md)

### ESP32
This guide teaches the ESP32 — the Wi-Fi/Bluetooth microcontroller behind most hobbyist and commercial IoT — for software engineers who've never programmed a microcontroller, framed throughout by contrast with the Raspberry Pi/Linux world: a microcontroller is a complete computer on a chip that runs one program forever with no OS, no shell, and tiny fixed memory, programmed by cross-compiling and flashing over USB. It covers the microcontroller mindset (bare metal, the no-OS development loop, serial debugging), the chip and the variant family (original ESP32 vs S2/S3/C3/C6/H2, boards vs modules vs chips, and the GPIO pin caveats that bite everyone), the three toolchains (Arduino, ESP-IDF, MicroPython — and PlatformIO), GPIO and the digital world (pull resistors, debouncing, interrupts, persistent storage), analog and buses (ADC, PWM, and I2C/SPI/UART with the sensor-library pattern), FreeRTOS and dual-core concurrency (why `delay()` is the cardinal beginner trap, the non-blocking `millis()` pattern, tasks and queues, the Wi-Fi core), Wi-Fi/Bluetooth/networking (station and AP provisioning, MQTT pub/sub as the IoT default, BLE, and the TLS-and-secrets security you can't skip), and power management and deep sleep (the 20,000× active-to-deep-sleep current drop that turns a half-day battery into a multi-year one). It then devotes a full part to five escalating, complete sample projects — blink, button+interrupt, a Wi-Fi/MQTT weather station, a deep-sleep battery sensor, and a web-controlled device — and closes with production concerns (crash backtraces, brownouts, OTA updates, Secure Boot and Flash Encryption, per-device provisioning, and an honest ESP32-vs-Pi "which do I reach for" verdict).

[Open on GitHub](https://github.com/s4njee/larning/blob/main/ESP32_STUDY_GUIDE.md)

### Git
This guide teaches Git from its object model up, on the premise that almost every confusing Git behavior becomes obvious once you know what Git is actually storing. It opens strong with the content-addressed object model (blobs, trees, commits, and tags as a hash-linked graph where a commit is an immutable snapshot identified by the hash of its contents including its parent), refs and HEAD and the reflog, the index as the staging snapshot, and the three-trees mental model — and now carries that same developed, why-first prose through the back half rather than sliding into command reference. Remotes are framed by the insight that a remote-tracking branch is a local snapshot not a live connection, which makes fetch-is-always-safe and the fetch-vs-pull distinction obvious; bisect is taught as O(log n) binary search over a monotonic history that you *run* (with git bisect run) rather than a diff you read; submodules' notorious confusion is dissolved by the fact that the parent stores only a pinned commit pointer into a separate history; and rewriting history is grounded in why it works (new commits, moved refs, because the hash includes the parent) and the publication boundary that makes it safe before a push and dangerous after. Branching and merging, rebasing and interactive rebase, cherry-pick, reset/restore/revert, stash, worktrees, tags, diff/log/blame, hooks, configuration, and a recovery-recipes and common-mistakes reference round it out, every command anchored to the object-model reason it behaves as it does.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/GIT_STUDY_GUIDE.md)

### GitHub Actions
This guide is a depth-first, pattern-first treatment of GitHub Actions for engineers building real CI/CD: the execution model (events, workflows, jobs, steps, runners), the workflow language, caching and matrix builds, composition with reusable and composite actions, and authoring your own JavaScript and Docker actions. It then goes deep on what bites in production — security hardening (the `pull_request_target` footgun, script injection, SHA-pinning actions, least-privilege `permissions`), deployment and release automation with OIDC to the cloud (no stored credentials), and operating runners at scale with Actions Runner Controller — before an honest comparison to GitLab CI, Jenkins, CircleCI, Buildkite, and Dagger. It closes with five copy-paste recipes and an end-to-end walkthrough from an empty repo to a reviewer-gated production pipeline, with examples spanning Node, Python, Go, and Rust.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/GITHUB_ACTIONS_STUDY_GUIDE.md)

### Golang for Python Developers
This guide helps Python developers understand Go through comparison rather than jargon. It focuses on the language features, concurrency model, packaging, and standard library patterns that make Go feel different but approachable.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/GOLANG_FOR_PYTHON_DEVS.md)

### GPU Programming with CUDA
This guide teaches GPU programming from the hardware up, organized around one thesis: a GPU is a throughput machine, and performance work is almost always memory work. It develops the SIMT execution model precisely (threads, 32-wide warps, blocks, grids, and what divergence actually costs), the streaming-multiprocessor hardware and occupancy (what it is, when it matters, when it doesn't), and the memory hierarchy as the guide's spine — registers, shared memory, L2, HBM, with round-number latencies, coalescing rules worked by example, and the roofline model as the mental frame for every optimization. A matmul ladder runs through the whole guide (naive → coalesced → shared-memory tiled → tensor cores), each rung justified with arithmetic. Streams and CUDA graphs, atomics and warp-level primitives, profiling as a discipline (Nsight Systems for the timeline, Nsight Compute for the kernel, and which stall reasons to believe), tensor cores and FP16/BF16/FP8 mixed precision, the libraries-first principle (cuBLAS/CUTLASS/CUB before your own kernel), Triton as the Python-embedded kernel language that became the ML default, and FlashAttention as the case study of memory-hierarchy reasoning producing a breakthrough. Closes with the ecosystem beyond CUDA (ROCm/HIP, SYCL, WebGPU) and how to get cheap GPU access for the labs.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/CUDA_STUDY_GUIDE.md)

### iOS Development
This guide is a depth-first treatment of iOS development in 2026 — for engineers who can program but have never shipped an iOS app, or who learned iOS years ago and need to understand the modern landscape. It starts with Swift the language (value types vs reference types and why structs are the default, optionals and safe unwrapping patterns, enums as algebraic data types with associated values and exhaustive pattern matching, protocols and protocol-oriented programming with `some` vs `any`, closures and trailing closure syntax, generics, error handling with typed throws, and property wrappers), then SwiftUI (declarative views as structs, the layout system — VStack/HStack/ZStack/LazyVStack/Grid/Spacer, Lists with swipe actions and pull-to-refresh, modifier chaining and why order matters, SF Symbols), state management in depth (the `@State`/`@Binding`/`@Observable`/`@Environment`/`@AppStorage` decision table, why `@Observable` replaced `ObservableObject` with granular property tracking, injecting shared services via environment), navigation (`NavigationStack` with type-safe `navigationDestination`, programmatic routing with a Router pattern using `NavigationPath` and route enums, tabs, sheets with `presentationDetents`), Swift 6 concurrency (async/await, structured concurrency with TaskGroup and `async let`, actors for thread-safe shared state, `@MainActor` for UI safety, `Sendable` protocol and region-based isolation, task cancellation), architecture (MVVM with Repository layer as the 2026 default, feature-based project structure, the path to modularization via Swift Package Manager), data persistence (SwiftData with `@Model` and `@Query` macros, auto-saving, relationships, when to use Core Data instead, Keychain for secrets), networking (URLSession with async/await, a typed APIClient pattern, `AsyncImage` for remote images), platform APIs (push notifications and APNs token registration, background tasks with BGTaskScheduler, biometric auth with LocalAuthentication, WidgetKit), UIKit interop (`UIViewRepresentable` and `UIHostingController` for bridging), testing (Swift Testing framework with `@Test` and `#expect`, snapshot testing, UI testing with XCUIApplication), tooling (Xcode shortcuts, SwiftUI previews and `_printChanges()` debugging, Instruments profiling, Swift Package Manager), and distribution (certificates and provisioning, TestFlight beta distribution, App Store submission checklist, the yearly WWDC cycle).

[Open on GitHub](https://github.com/s4njee/larning/blob/main/IOS_DEVELOPMENT_STUDY_GUIDE.md)

### Kafka & Stream Processing
This guide is the dedicated deep-dive the Distributed Systems guide's messaging part points toward, built on the thesis that Kafka is the replicated log made into infrastructure — every feature a consequence of the log abstraction. It covers topics, partitions, segments, and offsets from the storage format up; brokers and KRaft (Kafka 4.x is KRaft-only, with ZooKeeper as history you'll meet in older clusters); replication in real depth (ISR, high watermark, leader epochs, the acks/min.insync.replicas durability recipes and what each actually promises); producers (batching, idempotence via producer IDs and sequence numbers, transactions); consumers and rebalancing (eager vs. cooperative, the KIP-848 broker-driven incremental protocol, lag as the health metric); exactly-once semantics demystified as at-least-once plus dedup with the transaction coordinator and LSO mechanics; storage internals (segments, indexes, compaction, tiered storage) and the new share groups (queues for Kafka); Kafka Streams, Connect, and CDC with Debezium; schema management with compatibility modes; and an operations part on partition sizing, the metrics that page you, rebalance storms, and poison messages. Includes an end-to-end walkthrough of one record produce→replicate→consume with every knob named, and an honest when-not-to-use-Kafka section.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/KAFKA_STUDY_GUIDE.md)

### Kali Linux
This guide is a technique-first treatment of Kali Linux organized around the penetration-testing kill chain, written so that each phase explains how the attack actually works at the protocol and system level before naming the tools that automate it — because the tool changes yearly and the technique almost never does. After the distribution itself (why services are off by default, the injection-patched kernel, metapackages by workflow) and the methodology that gives every tool a home (enumerate before you exploit), it walks the chain: reconnaissance (passive vs active, certificate-transparency subdomain enumeration, discover-then-validate), scanning and enumeration (the SYN scan and the TCP handshake it exploits, why the service-version string is the hinge of the whole assessment, NSE, protocol-specific enumeration), web testing (the intercepting proxy as the master technique that dissolves client-side controls, content discovery by response-code inference, how SQL injection breaks out of the data context), password attacks (the online-vs-offline distinction that determines everything, hashes as a guessing race, wordlist craft), exploitation (Metasploit's exploit/payload/session model, why reverse shells call outward, research before firepower), Active Directory (compromise as path-finding through a trust graph, Kerberoasting and NTLM relay and AD CS abuse each as a distinct protocol attack, BloodHound), network attacks (sniffing, ARP spoofing, and name-resolution poisoning as exploits of protocols that trust rather than verify), wireless (the WPA2 handshake capture and offline crack demystified), reverse engineering (static vs dynamic, the decompiler, firmware as a container to carve open), forensics (timeline reconstruction, file carving, metadata vs content, found-is-not-executed), and cloud/secrets/social engineering — closing with reporting as the communication technique that turns access into an actionable finding, all under the discipline that authorization is the only thing separating the profession from a crime. The tool tables and Kali doc links are kept as reference, now surrounded by the technique the critique found missing.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/KALI_LINUX_STUDY_GUIDE.md)

### Kubernetes
This guide teaches Kubernetes day-1 and day-2 operations through its real organizing idea — an API plus a fleet of reconciling controllers — with the foundational concepts developed as explanatory prose rather than enumerated. It opens with cluster architecture as two halves that communicate through one component (the API server as the only thing that talks to etcd, workers that pull rather than get pushed, the scheduler's filter-then-score decision, the kubelet as the thing that actually runs your workloads), the API model where the spec/status split *is* the reconciliation loop made concrete, and the relationship fields (labels as the queryable identity Services select on, selectors as contracts dangerous to change, owner references for cascading deletion, finalizers as the cause of stuck Terminating), then namespaces as a name scope that is explicitly not a security boundary. It develops the workload types (the Pod and its shared namespaces, the pause container, init vs sidecar containers, and the readiness-vs-liveness-vs-startup probe distinction whose confusion causes cascading outages, plus graceful shutdown via terminationGracePeriod and preStop), ReplicaSets/Deployments and rollout strategy, StatefulSets, and DaemonSets/Jobs/CronJobs. Further phases cover configuration and state (ConfigMaps/Secrets, the config-change rollout pattern, persistent volumes and storage classes), services and discovery (the Service types, DNS, Ingress and Gateway API), scheduling and resource management (requests/limits and QoS, affinity and taints, HPA/VPA autoscaling, quotas), operations and debugging (the common failure modes, the debug toolkit, node operations, and worked incident walkthroughs), API access and policy (Service Accounts and RBAC, CRDs and operators, admission control, Helm/Kustomize packaging), observability, and production patterns — every concept paired with runnable manifests, practice exercises, and official doc links.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/k8s/KUBERNETES_STUDY_GUIDE.md)

### Kubernetes (Advanced)
This guide picks up where the Kubernetes fundamentals, security, and networking guides leave off — covering the control plane's internals and how to program against it. It starts with etcd deeply (performance, sizing, backup/recovery), the API server (watches, the watch cache, API priority and fairness, admission webhook latency), and the reconciliation model that underlies every controller. It then covers the API machinery and extension model (GVR, CRDs with validation and subresources, Server-Side Apply, CEL admission policies), building operators with Kubebuilder/controller-runtime (the reconcile loop, owner references, idempotency, testing with envtest), advanced scheduling (the scheduling framework plugin model, pod topology spread, GPU/accelerator scheduling with device plugins and DRA, the descheduler), advanced workloads and scaling (PodDisruptionBudgets, VPA, KEDA for event-driven scale-to-zero, and progressive delivery with Argo Rollouts), multi-tenancy and resource governance (ResourceQuotas, LimitRanges, policy engines — Kyverno, OPA/Gatekeeper, CEL — and cost management), GitOps with Argo CD and Flux (repo structure, Kustomize overlays, image automation, drift detection), multi-cluster and federation (Cluster API, hub-and-spoke, cross-cluster networking), supply-chain security (Cosign image signing, SBOMs, SLSA, Trivy, registry restrictions) and production operations (upgrade strategy, disaster recovery, control-plane observability), and platform engineering (golden paths, Backstage, abstraction CRDs, and the maturity model).

[Open on GitHub](https://github.com/s4njee/larning/blob/main/k8s/ADVANCED_KUBERNETES_STUDY_GUIDE.md)

### Kubernetes Security
This guide is a prose-first, manifest-driven treatment of Kubernetes security organized around one thesis — there is no single control that secures a cluster, so security is the discipline of stacking independent layers so that any one failure is contained rather than catastrophic — and a recurring question: when this layer fails, what is the blast radius and what catches it next? It builds the threat model (the 4Cs, why managed means managed uptime not managed security, the API server as the chokepoint you must never bypass, defaults that ship open), then walks the request lifecycle's four gates: authentication (users live outside the cluster and ServiceAccounts inside it, OIDC as the production human-access pattern, the automounted Pod token as pure attack surface, cloud workload identity over static keys), authorization (RBAC's additive-only model, least privilege as the only model the system supports, and the permissions that are secretly cluster-admin — Pod create, Secret read, escalate/bind/impersonate, wildcards), and admission control (the mutate-then-validate phases, Pod Security Admission with its warn/audit/enforce rollout, and Kyverno/OPA for the policies PSA can't express). It then covers workload hardening (a container is a process sharing the host kernel, the full hardened securityContext line by line with the attack each setting removes, privileged-as-root-on-the-node, gVisor/Kata for untrusted code), network security (default-deny NetworkPolicy with the DNS gotcha and the CNI-must-enforce caveat, the service-mesh-mTLS question, namespaces as a soft boundary and the soft-vs-hard multi-tenancy spectrum), secrets (base64 is not encryption, encryption-at-rest with KMS v2, external stores vs Sealed Secrets and the operational trade), supply chain (minimal images, scanning in CI and continuously, cosign signing with admission-time verification and digest pinning), and audit/detection/response (the audit log as flight recorder, Falco for the post-exploitation activity prevention can't see, and the incident moves decided in advance). It closes with an assembled hardened-cluster posture and seven hands-on labs that each demonstrate an attack and the control that stops it.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/k8s/KUBERNETES_SECURITY_STUDY_GUIDE.md)

### Linux Fundamentals
This guide is a depth-first treatment of how Linux actually works, for engineers who use it daily but have never looked beneath `ps`, `kill`, and `chmod` — and it's the substrate under much of the rest of the repo (Docker, Kubernetes, Kali, the language runtimes). It builds on one principle (everything is a file, including process and kernel state in `/proc` and `/sys`), then works through the process model (`fork`/`exec`, the process tree, states, zombies, threads, and what exit code 137 means), file descriptors and I/O (stdin/stdout/stderr, the `nofile` limit, and how `epoll` watching many fds is the mechanism under every event loop — Node's, Go's, Python's), signals (SIGTERM vs SIGKILL, graceful shutdown, and the PID 1 problem in containers), users/permissions/capabilities (the model under `runAsNonRoot` and `securityContext`), the filesystem in depth (inodes, hard vs symbolic links, mounts, tmpfs, "disk full but `df` disagrees"), and systemd (units, journald, timers, and how its resource directives map to cgroups). It then reaches the chapter the repo builds toward — namespaces and cgroups, showing that a container is just a Linux process with isolation (what it can see) and limits (what it can use), with the OOM killer as the SIGKILL behind a Kubernetes exit-137 — followed by the network stack from the OS side (veth pairs, bridges, iptables/NAT, how Services are DNAT rules) and a diagnostic toolkit organized as a workflow, ending on `strace` as the universal debugger.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/LINUX_FUNDAMENTALS_STUDY_GUIDE.md)

### Linux Networking
This guide is a practical, command-line-first treatment of Linux networking for engineers who need to debug real hosts, VMs, containers, and lab networks. It focuses on the modern operational toolkit: `ip` for links, addresses, routes, rules, neighbors, namespaces, and monitoring; `ss` for sockets, listeners, TCP state, queues, and process ownership; `nc`/netcat for raw connectivity testing, ad hoc listeners, UDP smoke tests, Unix sockets, and trusted-network byte streams; and `iperf3` for TCP, UDP, reverse, bidirectional, parallel-stream, JSON, and zero-copy throughput tests. It includes hands-on namespace/veth labs, real troubleshooting playbooks for broken services, wrong routes, DNS failures, filtered ports, MTU black holes, and slow links, plus a practical file-transfer cookbook comparing `scp`, `rsync`, `tar` over SSH, tar piped through netcat, and direct netcat by security, resumability, metadata preservation, repeatability, and speed.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/LINUX_NETWORKING_STUDY_GUIDE.md)

### LLM Application Development
This guide covers building applications on top of large language models, focused on the patterns that hold across providers and frameworks rather than any single API's surface. It works through the mental model, the anatomy of an API call, prompt-engineering patterns, structured output, tool use (function calling), retrieval-augmented generation, agents, streaming, context-window management, and caching, with examples in Python and pseudocode.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/LLM_APP_DEV_STUDY_GUIDE.md)

### Networking Fundamentals
This guide is a depth-first foundation for how computer networks actually work: layered architecture, Ethernet and ARP, IP and routing, NAT and firewalls, TCP and UDP, DNS, the networking view of TLS, and HTTP/1.1 vs. 2 vs. 3 — followed by load balancing, modern realities (CDNs, eBPF, service mesh), a diagnostic toolkit, a hands-on recipe for connecting two Linux machines directly with an Ethernet cable, the real-world problems that keep recurring, and an end-to-end walkthrough of what happens when you type a URL into the browser.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/NETWORKING_FUNDAMENTALS.md)

### Next.js
This guide covers Next.js as a full-stack React framework, with an emphasis on the parts that matter when you are building production apps. It focuses on routing, rendering strategies, data loading, API handling, and the tradeoffs that come with framework-driven React.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/NEXTJS_STUDY_GUIDE.md)

### Observability
This guide goes beyond "install Prometheus" into how to think about production signal: metrics with PromQL, structured logs, distributed tracing with OpenTelemetry, events and profiling, SLI/SLO/SLA with multi-window burn-rate alerts, alert design via RED and USE, dashboards that earn their screen real estate, the tooling landscape honestly, and the operational realities (cost, cardinality, sampling). It closes with recipes: instrumenting a Go HTTP service end to end with OpenTelemetry, writing a burn-rate SLO alert in PromQL, writing a runbook that actually helps oncall, and an end-to-end walkthrough of a production observability stack.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/OBSERVABILITY_STUDY_GUIDE.md)

### PostgreSQL Feature Reference
This is the reference half of the PostgreSQL pair — a broad, runnable feature catalog spanning the type system, table definition, constraints, every index type, query features (CTEs, window functions, LATERAL joins), JSON/JSONB, arrays, ranges, full-text search, transactions and isolation, locking, partitioning, functions and triggers, LISTEN/NOTIFY, row-level security, roles, and psql — now written as an *explained* reference rather than a bare one. A mental-model opener grounds everything in the two choices (process-per-connection, MVCC plus WAL) that make Postgres's behavior consistent rather than arbitrary, and each major area now opens with a developed teaching paragraph that frames the decision the reference entries beneath it serve: why choosing the most specific data type moves correctness into the engine; why a database-enforced constraint holds against every writer where application code doesn't; how to choose an index type and modifier from the query shape rather than reflexively; how isolation level trades consistency against retry rate and why a forgotten open transaction blocks VACUUM cluster-wide; when partitioning's complexity actually pays off; why row-level security is the un-forgettable tenant filter; and how the roles-are-just-roles model builds a least-privilege access design. The deep internals (EXPLAIN, the planner, VACUUM and wraparound, WAL and durability, replication, tuning) live in the Advanced PostgreSQL sibling, which this guide cross-links throughout.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/POSTGRES.md)

### Advanced PostgreSQL
The depth-first companion to the Feature Reference: the engine beneath the SQL, pitched at a senior database role. Its thesis is that almost every Postgres performance and operations problem is a consequence of two design choices — MVCC and WAL — and it builds the whole arc from there: the storage/MVCC engine (pages, tuples, xmin/xmax, HOT updates, TOAST, bloat), WAL/checkpoints/durability, VACUUM/autovacuum/transaction-ID wraparound, the cost-based planner and statistics, reading `EXPLAIN` (scan and join strategies), index internals and strategy (B-tree/GIN/GiST/BRIN/SP-GiST/Hash, partial/covering/expression), locking and concurrency at scale, connections and pooling (PgBouncer modes), the performance tuning ladder, partitioning at scale, replication and high availability, backup/PITR/disaster recovery, observability (`pg_stat_statements`, wait events), production patterns and pitfalls, benchmarking with `pgbench`, seven worked performance recipes, and decision trees.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/ADVANCED_POSTGRES.md)

### PostgreSQL Extensions in Production
The third member of the PostgreSQL set, focused on the ecosystem that makes Postgres a platform rather than just a database. It is opinionated about production-readiness: every extension gets a verdict (essential / strong / situational / specialist / caution), an explanation of how it actually works under the hood, the production caveats that don't appear in the README, its managed-service availability, and a documentation link. It covers how extensions physically work (control files and migration-script chains, contrib vs third-party, trusted vs untrusted, `shared_preload_libraries`, the pgrx/pg_tle packaging landscape, and what is *not* an extension), a day-one shortlist, observability and ops (`pg_stat_statements` read correctly, `auto_explain`, `pgaudit`, `pg_repack`'s shadow-table mechanics and `pg_squeeze`, `hypopg`, `amcheck`, the autoprewarm trick, the PoWA deep-performance stack, and the WAL/page forensics kit), indexing/search/data-types (trigram mechanics in `pg_trgm`, exclusion constraints via `btree_gist`, `ltree`, RUM, pgroonga for CJK, and the UUIDv7 story), geospatial (PostGIS's geography-vs-geometry decision, pgRouting, H3 hex analytics), time-series (TimescaleDB's four pillars and licensing reality vs. the native partitioning + BRIN + pg_partman stack), vectors and AI (HNSW vs IVFFlat trade-offs, the filtered-search gotcha, pgvectorscale/ParadeDB/VectorChord, and why in-database inference deserves caution), a new analytics section (pg_duckdb, columnar access methods, HyperLogLog rollups), JSON/graph/API layers (pg_jsonschema, Apache AGE, pg_graphql, and the case against in-database HTTP), security (pgcrypto done right, anonymizer, pgsodium/Vault, pg_tde), foreign data wrappers (push-down verification discipline), Citus scale-out, scheduling and queues (`pg_cron`, `pg_partman`, pgmq's transactional-enqueue advantage), procedural languages and dev tooling (plpgsql_check, pgTAP, pldebugger, the pg_hint_plan debt warning), and replication/CDC (logical decoding plugins, slot-pinning discipline, pg_failover_slots and its happy obsolescence in PG17) — closing with an expanded availability matrix and a tiered decision guide.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/POSTGRES_EXTENSIONS.md)

### Python Concurrency
This guide is the map and decision guide for Python concurrency — it covers the whole landscape, explains *why* each model behaves as it does, and helps you choose, built around one master distinction: is the work I/O-bound (overlap the waiting with threads or async) or CPU-bound (parallelize the computing with processes)? It works through the mental model (concurrency vs parallelism, the I/O-vs-CPU classification), the GIL in depth (what it is, why it exists, what it blocks and releases, how the switch happens), threading (the niche, races and locks, the synchronization toolkit), multiprocessing (true parallelism, start methods — fork/spawn/forkserver — pickling, shared memory, and when a GIL-releasing native library beats it), `concurrent.futures` as the unifying layer (one API for threads and processes, `submit` vs `map`, the silent-exception trap, pool sizing), and a survey of `asyncio` (cooperative single-threaded concurrency, the key primitives, the ecosystem tax — handing off to the Asyncio & aiohttp guide for mastery). It then gives a full decision framework (a decision tree plus the ThreadPoolExecutor-vs-asyncio fork), the model-independent concurrency patterns (bounded concurrency, producer/consumer with backpressure, fan-out/fan-in, pipelines, timeouts and cancellation), a 2026 look at the future (free-threaded CPython and per-interpreter sub-interpreters — the GIL's end), and closes with copy-paste recipes and the eight pitfalls that cost the most debugging time. For the depth-first dive into async itself, it hands off to the companion Asyncio & aiohttp guide; for the runtime underneath, the Advanced Python guide.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/PYTHON_CONCURRENCY.md)

### Python vs Node.js Async
This guide is an opinionated, side-by-side comparison of asynchronous programming in Python and Node.js — what each gets right, what each gets wrong, and which to choose when. Its thesis: both made the same bet (a single-threaded cooperative event loop) but arrived from opposite directions — Node async-from-birth and uniform, Python async-bolted-on and fragmented — and that history explains every practical difference. It works through the execution models side by side (libuv runs under both, via uvloop on the Python side), the deepest semantic split (eager JavaScript async functions vs lazy Python coroutines, and their opposite failure modes for a forgotten `await`), the object models (one Promise vs coroutine/Task/Future, and Python's first-class cancellation vs Node's bolted-on `AbortController`), the colored-function problem (and why Python's sync/async ecosystem split makes it worse), blocking the event loop (Node's decisive structural advantage — you can't accidentally grab a blocking I/O library), structured concurrency (Python's decisive win — `TaskGroup`, `ExceptionGroup`, timeouts that actually cancel, built-in primitives), CPU-bound parallelism (the GIL vs Worker Threads), and performance and ecosystem honestly. It closes with a dimension-by-dimension scorecard, an opinionated summary, and a when-to-choose-which decision framework. Pairs with the Asyncio & aiohttp, Advanced Node.js, and Python Concurrency guides.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/PYTHON_VS_NODEJS_ASYNC_STUDY_GUIDE.md)

### Qt
This guide teaches Qt 6 as a complete toolkit for building cross-platform desktop and embedded applications, with runnable code in every section and dual C++/PySide6 examples where they differ. It is organized as a layered learning path: the `Qt Core` foundation (the event loop, signals/slots, the meta-object/moc system, and Qt 6 bindable `QProperty`), the model/view architecture that is Qt's real centerpiece (custom `QAbstractItemModel`s, `QSortFilterProxyModel`, delegates, and the same model feeding both a `QTableView` and a QML `ListView`), and the two UI stacks — classic `Qt Widgets` for dense desktop tools and the declarative `Qt Quick`/QML stack (Quick Controls, Layouts, and styling via QSS and the Material/Fusion styles). It then covers the integration layers most real apps need — `Qt Network`, `Qt SQL`, concurrency with both `Qt Concurrent` and the `QThread` worker-object pattern, and testing with `Qt Test` and `QSignalSpy` — plus the specialized modules for media, SVG, serial-port device I/O, and WebSockets. A dedicated production phase tackles the last mile most tutorials skip: internationalization (`tr()`, Qt Linguist, `QTranslator`) and deployment (CMake, `windeployqt`/`macdeployqt`, `pyside6-deploy`). It closes with three integrated capstone projects and three focus tracks — C++ desktop, QML/embedded, and a thorough PySide6 path.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/QT_STUDY_GUIDE.md)

### Quantum Computing
This guide is a practitioner-oriented tour of quantum computing focused on the algorithms that actually matter and the intuitions behind why quantum mechanics lets them outperform classical machines, not a physics or linear-algebra course. It builds from the three pillars (superposition, entanglement, interference) through Grover's proven quadratic search speedup and Shor's proven exponential factoring speedup (the reason post-quantum cryptography exists), quantum simulation as the field's most credible near-term killer app, and an honestly-hedged tour of quantum machine learning and QAOA-style optimization where the advantage remains unproven. Quantum error correction gets treated as the actual engineering bottleneck it is, followed by a taxonomy of speedup types, quantum linear algebra (HHL and its caveats), post-quantum cryptography as the most immediately actionable topic for working engineers, the physical hardware platforms, and the software stack from Qiskit down to real devices. It closes with a frank "what quantum computers will not do" section, a milestone timeline, and a week-by-week learning roadmap for engineers (not physicists) who want to go from zero to running a real circuit on IBM's hardware.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/QUANTUM_COMPUTING_STUDY_GUIDE.md)

### RAG & Retrieval Engineering
This guide treats RAG as what it actually is — a search problem wearing an AI costume — on the thesis that retrieval quality, not the LLM, is where these systems fail. It goes deep on embeddings (bi-encoders vs. cross-encoders, MTEB and its overfitting caveats, Matryoshka and quantized embeddings), chunking as a real design decision (fixed/recursive/semantic/structural, contextual retrieval as a named technique), why lexical search never became legacy (BM25 mechanics, hybrid search, Reciprocal Rank Fusion), ANN indexes and their tuning (HNSW's M/ef knobs, IVF, product quantization, and the filtered-search problem every engine competes on), reranking (cross-encoders, ColBERT late interaction, the retrieve-broadly-then-rerank pattern), the storage decision worked honestly across pgvector, dedicated vector stores, and search engines, query understanding and agentic retrieval (rewriting, HyDE, multi-query, and how an agent that iterates searches changes index design), and — as the guide's spine — a full part on evaluation: recall@k/MRR/nDCG, building a labeled eval set with LLM-assisted labeling, RAGAS-style end-to-end metrics with caveats, and regression-testing retrieval in CI. Production concerns (ingestion sync, embedding versioning, tenant-scoped access control, citations, cost) and a worked query walkthrough close it out.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/RAG_STUDY_GUIDE.md)

### Raspberry Pi Zero 2 W — Embedded Linux
This guide teaches embedded Linux through the Raspberry Pi Zero 2 W with board-on-desk practicality across the full surface — hardware, the boot process, headless setup, device trees and overlays, GPIO, the bus protocols, the camera, power management, systemd, networking, cross-compilation, real-time tuning, security, and practical projects — now with the hardware-interfacing protocols developed conceptually rather than presented as commands to run. I2C is explained as a two-wire addressed multi-drop bus (why i2cdetect walks addresses, why open-drain pull-ups mean devices can't damage each other, why it's economical on pins but slower), SPI as the four-wire full-duplex speed protocol whose separate MOSI/MISO lines and per-device chip-select trade pins for tens-of-megahertz throughput (and whose mode mismatch is the source of 'I get garbage'), and UART as the clock-free asynchronous point-to-point protocol where both sides must agree on baud rate in advance and a mismatch is the classic garbage bug — with a clear when-to-use-which sorting across all three. The Pi-vs-ESP32 foil sharpens the Linux-computer-versus-microcontroller distinction throughout, and every concept is grounded in runnable commands, config, and code plus a mastery checklist and escalating projects.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/RASPBERRY_PI_STUDY_GUIDE.md)

### Redis
This guide is a comprehensive treatment of Redis — every data structure (strings, hashes, lists, sets, sorted sets, streams, HyperLogLog, bitmaps), persistence, replication, Sentinel, Cluster, security, and performance — now grounded in the why-it-works mechanism beneath the recipes rather than recipe-forward. The mental model develops the master fact that Redis processes commands one at a time on a single thread, and traces every consequence: sub-millisecond latency, the free atomicity that makes counters, locks, rate limiters, and reliable queues correct *because* of single-threaded execution, and the sharp edge that a single slow O(N) command blocks the entire server (the operational rule that makes O(1)/O(log N) data-structure choice a stability concern, not just a speed one). The data-structure sections explain the internals that set their performance — sorted sets as a skip list plus a hash table, which is why a top-ten leaderboard query over a million players touches ~20 nodes not a million — and the systems chapters develop the real mechanisms: RDB's fork-and-copy-on-write snapshots versus AOF's redo-log with its fsync durability dial (the same WAL tradeoff every storage engine makes), and Cluster's 16,384 hash slots as the indirection that lets resharding move slots instead of rehashing every key, with hash tags as the data-locality escape hatch. Pub/sub, transactions and Lua scripting, pipelining, the common patterns, client best practices, and a common-mistakes catalog round it out, every pattern now paired with the mechanism that makes it work.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/REDIS_STUDY_GUIDE.md)

### Rust for Python Developers
This guide helps Python developers get comfortable with Rust by focusing on the differences that matter most: ownership, borrowing, safety, and performance. It is designed to make Rust feel less mysterious and more like a careful, deliberate tool for systems work.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/RUST_FOR_PYTHON_DEVS.md)

### SQLite
This guide is a comprehensive deep dive into SQLite — the most widely deployed database engine in the world. It covers SQLite's architecture (B-trees, the pager, the virtual machine), its manifest type system, concurrency and locking (WAL mode vs rollback journal), performance characteristics, and practical embedded use cases.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/SQLITE_STUDY_GUIDE.md)

### Scala
This guide teaches Scala properly as the functional language it is, while explaining the JVM it compiles down to — it's the repo's coverage of the JVM managed runtime, and where the Compiler Internals guide's HotSpot/tiered-JIT references point. Built on one through-line — Scala fuses FP and OO into a powerful type system, but it all erases to JVM bytecode on HotSpot — it works through the mental model (FP+OO fusion, Scala 3, Java interop), the JVM runtime underneath (bytecode, the tiered C1/C2 JIT and warmup, deoptimization, generational/G1/ZGC garbage collection, type erasure and autoboxing and why immutable functional code is affordable), and then the functional features in depth: expressions/immutability/referential transparency (val vs lazy val vs def), algebraic data types and pattern matching (case classes, sealed traits, enums, exhaustivity as a compile-time checklist), functions as first-class values (higher-order functions, currying, composition, by-name params), functional error handling (Option/Either/Try and for-comprehensions as flatMap/map sugar that works for any monad), the immutable collections library and laziness (structural sharing, views, LazyList), type classes via given/using and the Cats hierarchy (Functor/Applicative/Monad, Semigroup/Monoid — ad-hoc, retroactive polymorphism), the type system at depth (variance, higher-kinded types, opaque/union/intersection types), and functional effects and concurrency with Cats Effect and ZIO (the IO monad making effects pure values, fibers, structured concurrency), closing on tooling (sbt, Metals, worksheets) and when Scala is the right tool. Quizzes and build-time mermaid diagrams (the JVM tiered-JIT pipeline, the ADT sum-of-products, for-comprehension desugaring, the Cats type-class hierarchy, the fiber runtime) throughout.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/SCALA_STUDY_GUIDE.md)

### SvelteKit
This guide explains SvelteKit as a modern app framework with a lightweight, ergonomic developer experience. It focuses on the patterns you need to understand to build real apps: routing, data loading, component structure, and deployment flow.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/SVELTEKIT_STUDY_GUIDE.md)

### Swift
This guide is a depth-first treatment of the Swift programming language in 2026. It covers the type system, memory management (ARC), optionals, closures, enums and pattern matching, protocols, generics, collections, strings, error handling, Swift 6 concurrency, macros, result builders, ownership, and noncopyable types.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/SWIFT_STUDY_GUIDE.md)

### systemd
This guide is a depth-first treatment of systemd for system administrators who use it daily but have only ever copied `.service` snippets — built on one model: everything is a *unit*, units declare *dependencies*, and the manager (PID 1) tracks every process through *cgroups*. It works through the mental model and why systemd replaced SysV init, unit-file anatomy and the `/etc` vs `/usr/lib` override hierarchy with drop-ins (`systemctl edit`/`cat`/`daemon-reload`), service units in depth (the `Type=` readiness contract — simple vs notify vs oneshot — the `Exec*` family, `Restart=` and the start limiter that stops crash loops, and the service state machine), the two orthogonal dependency axes (requirement `Wants`/`Requires` vs ordering `Before`/`After`, the pairing that fixes flaky-on-reboot races) and targets as runlevel replacements (and how `enable` is just a `.wants/` symlink), the boot process with `systemd-analyze blame`/`critical-chain`, the journal (structured field queries, persistent storage, rotation, rate limiting), timers as a better cron (`OnCalendar`, monotonic triggers, `Persistent=`), resource control via the cgroup slice hierarchy (`MemoryHigh` throttles vs `MemoryMax` kills, `CPUQuota` cap vs `CPUWeight` share, `systemd-cgtop`), sandboxing and hardening (`DynamicUser`, `ProtectSystem`, capabilities, seccomp, and the `systemd-analyze security` exposure score), socket/path/on-demand activation (how it de-races boot), and a day-to-day operator's toolbox ending in an end-to-end hardened-service walkthrough. Quizzes and build-time mermaid diagrams (the boot target chain, the service state machine, the cgroup slice tree, the socket-activation sequence) throughout.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/SYSTEMD_STUDY_GUIDE.md)

### Terraform
This guide treats Terraform as an infrastructure workflow tool, not just a syntax exercise. It emphasizes how providers, state, modules, plans, and applies fit into a safe and repeatable provisioning process.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/TERRAFORM_STUDY_GUIDE.md)

### Testing
This guide is a comprehensive, cross-language treatment of software testing for working engineers. It covers the mental model of tests as executable confidence, the practical test portfolio (unit, integration, component, contract, E2E, property, fuzz, performance, security, accessibility, and exploratory testing), and the mechanics of writing durable tests in Python, Node.js, and Go. It then goes deep on enterprise testing strategy: shift-left and shift-right, test ownership, CI/CD gates, flaky-test management, test data and environments, contract testing between services, non-deterministic systems, legacy-code characterization, and checklists for adding tests to real production work.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/TESTING_STUDY_GUIDE.md)

### TypeScript
This guide teaches TypeScript's type system through one load-bearing mental model — types are sets of values, so union is set union, assignability is subset, never is the empty set, and structural typing means shape not name — and then walks the full type system on that spine: annotations and inference, unions and intersections, literal types and narrowing, type guards and control-flow analysis, function overloads and this-typing, generics, the utility types, conditional and mapped and template-literal types, the infer keyword, enums vs union literals, branded/nominal types, declaration merging, the unknown/never/any/void special types, type-level programming, and classes. It now closes the scope gap beyond the type system with a real-world chapter on build, tooling, and ecosystem: the erasure model (type-checking and transpilation as two separate programs, types gone at runtime) and why that means tsc checks while a fast bundler builds — so 'it builds' is never 'it type-checks' and CI must run tsc --noEmit explicitly; the @types/DefinitelyTyped ecosystem and declaration files for typing untyped JavaScript; type-aware linting with typescript-eslint catching floating promises and leaking any; and the runtime-validation boundary where types run out and a schema library like Zod gives you a runtime guard and an inferred static type from one definition. Plus tsconfig's options that matter, a pitfalls catalog, and a what-to-use-when quick reference.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/TYPESCRIPT_STUDY_GUIDE.md)

### Unreal Engine
This guide is a depth-first treatment of Unreal Engine 5 for engineers who want to understand how the engine actually works, not just where the buttons are. It frames the engine as four layers (the UObject core, engine subsystems, the gameplay framework, and your game) and makes the case that the reflection system at the bottom — what `UCLASS`/`UPROPERTY`/`UFUNCTION` register — is what powers garbage collection, serialization, the editor's Details panel, Blueprint exposure, and network replication all at once. It covers the editor and project anatomy (what's source-of-truth vs generated, why `Content/` is unmergeable, PIE's lies), the Actor/Component object model and lifecycle, and the gameplay framework in depth (why a Controller possesses a Pawn, and what GameMode/GameState/PlayerState own) with a worked respawn example. It goes deep on both scripting surfaces — Blueprints (the VM, the graph, interfaces vs event dispatchers, the hard-reference memory trap) and Unreal C++ (the mandatory macros, the `FString`/`FName`/`FText` trilemma, the two memory worlds and why a `UObject*` without `UPROPERTY` is a use-after-free, `TObjectPtr`/`TWeakObjectPtr`/`TSubclassOf`) — and the "C++ base, Blueprint derived" pattern that unifies them. It then treats UE5 rendering (deferred shading and the G-buffer, Nanite, Lumen, Virtual Shadow Maps, and the baked-vs-Lumen decision, with `stat unit` as the triage tool), materials (PBR inputs, blend/shading models, and Material Instances as the reuse-and-runtime lever), animation (the AnimBP's two graphs, state machines, blend spaces, montage notifies), physics and collision (channel-based responses, Overlap vs Hit, traces, and why character movement isn't raw physics), Enhanced Input, UMG/Slate UI, MetaSounds audio, and networking (server authority, `HasAuthority`, the actor roles, property replication with RepNotify, the three RPC types, and client prediction/reconciliation). It closes with cooking and packaging, a measure-first performance and profiling chapter (Unreal Insights, the usual CPU/GPU culprits), a recipes section (pickups, a reusable health component, subsystems instead of singletons, data-driven design), a beginner-oriented additional-resources reading list, and a mastery checklist.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/UNREAL_ENGINE_STUDY_GUIDE.md)

### Vim
This guide is a depth-first treatment of Vim built around one idea: modal editing is a *language*, not a list of shortcuts, and learning its grammar is how you edit faster than any point-and-click editor. It starts with the mental model (why modes exist, the map between Normal/Insert/Visual/Command-line, and why your hands never leave the home row), then teaches the grammar at the core of everything — operators (verbs) composed with motions and text objects (nouns), multiplied by counts and the linewise-doubling rule. From there it goes deep on each piece: precision motions (word/WORD, find-and-till, search-as-motion, marks and jumps, and the inclusive/exclusive quirk behind `cw`), the text objects that make editing feel telepathic (`ci"`, `dap`, `cit`, inner vs. around), changing text and the register system (the `.` repeat command, the yank and black-hole registers that stop your clipboard betraying you, undo as a tree), search and its underused power tools (`:substitute` with ranges and capture groups, the `:global` command, and the `*`-`cgn`-`.` answer to multiple cursors), and macros for repetitive multi-step edits. It closes with the multi-file model (buffers vs. windows vs. tabs, `:argdo`, Ex ranges, filtering through shell commands), a sane starter config, the plugin and Neovim ecosystem, Vim mode in VS Code/JetBrains/your shell, an honest "when not to go all-in," a concrete path to learning it in a month, and a speed cookbook of edits that are reflexes in Vim and a chore everywhere else.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/VIM_STUDY_GUIDE.md)

### Vue
This guide introduces Vue through the lens of practical app building, focusing on the parts most developers reach for every day. It covers component structure, reactivity, templates, and the broader ecosystem so the framework feels organized rather than fragmented.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/VUE_STUDY_GUIDE.md)

### Web Application & LLM Security
This guide is a prose-first, defensive treatment of web and LLM security as a single discipline — control your trust boundaries — built on the mindset that all input from across a boundary is hostile and the model's output is just another such input. It develops each bug class as a mechanism rather than a checklist entry: the security mindset (trust boundaries, defense in depth, least privilege, fail securely, STRIDE threat modeling), then the high-impact web categories with code for each — broken access control and IDOR (authorization at the data layer, 404-not-403), injection as one shape across SQL/shell/LDAP with the structural code-vs-data fix, XSS and the named escape hatches, SSRF and CSRF as confused-deputy attacks, authentication as a lifecycle (Argon2id, session fixation, the enumeration and reset edges where takeovers actually happen), and misconfiguration/crypto/components as the failures around the code. It covers the web platform model (Same-Origin Policy, the CORS-relaxes-not-adds-security correction, the cookie storage tradeoff that picks which attack you defend against, nonce-based CSP as the safety net), then extends the same discipline to LLMs: prompt injection as the unsolvable master vulnerability (direct and indirect) that erases the data/instruction boundary, insecure output handling (the model as an untrusted user inside your backend), excessive agency (the model's privileges are the attacker's, so security collapses to how tightly you scoped its tools), the Markdown-image exfiltration channel, tenant-scoped RAG retrieval, and the abuse long tail. It closes with the two pillars (validate input, encode output), pipeline security, and four build-break-fix capstone labs.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/WEB_LLM_SECURITY_STUDY_GUIDE.md)

### WebAssembly
This guide treats WebAssembly as what it is — a portable, sandboxed, low-level bytecode for a virtual stack machine, a compilation target rather than a language — and covers both the runtime (filling the repo's portable-managed-runtime gap, a concrete instance of the Compiler Internals theory) and where it's genuinely the right tool. Built on one through-line — any language compiles to one sandboxed module that runs at near-native speed anywhere — it works through the mental model and the four design goals (fast, safe, portable, compact, and not just for the browser), the binary format and stack machine (the four numeric types, the WAT text format, validation as the cheap-safety proof, and streaming/tiered compilation), the linear-memory model (one byte array, pointers as offsets, bounds-checked isolation, shared memory/threads/atomics and SIMD), the capability-based deny-by-default security model (memory isolation and control-flow integrity that make WASM a security primitive displacing containers at the edge), the host boundary (imports/exports and the load-bearing fact that only numbers cross — so strings go through linear memory via allocate-copy-call, hidden but not eliminated by wasm-bindgen/Emscripten), WASI (a capability-secured portable syscall layer with preopened-directory sandboxing, Preview 1 vs Preview 2), the component model and WIT (typed, language-agnostic composition that lets a Rust component call a Go one), languages and toolchains (Rust/C/C++ as natural GC-free fits, the GC problem and WasmGC, TinyGo/AssemblyScript/Emscripten), the runtime landscape (browser engines, Wasmtime/Wasmer/WasmEdge/wazero, edge and plugin embedding), and the honest performance story (near-native but boundary-dominated for chatty workloads — when to reach for it and when not). Quizzes and build-time mermaid diagrams (any-language-to-module-to-any-runtime, the compile/instantiate pipeline, the sandbox capability boundary, crossing the boundary with a string, and the component model) throughout.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/WEBASSEMBLY_STUDY_GUIDE.md)

### WebGL and OpenGL
This guide is a depth-first treatment of the GL family of graphics APIs, using WebGL as the browser-based teaching environment for OpenGL and OpenGL ES concepts. It builds the mental model of GL as a state machine for feeding a programmable rasterization pipeline, then works through context setup, shaders, buffers, attributes, uniforms, VAOs, matrices, cameras, textures, samplers, framebuffers, depth, stencil, blending, transparency, instancing, performance, debugging, context loss, library choices, and how WebGL calls map back to native OpenGL.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/WEBGL_OPENGL_STUDY_GUIDE.md)

### WebGPU
This guide is a depth-first introduction to WebGPU, the modern explicit GPU API for the web. It explains what WebGPU is, why it exists, how it maps to modern graphics APIs, and how it compares with WebGL across state management, shaders, resources, compute, performance, compatibility, and developer experience. It then gives a practical primer on using WebGPU: adapters and devices, canvas configuration, buffers, textures, bind groups, render pipelines, WGSL, drawing a triangle, uniforms, render loops, compute shaders, debugging, performance practices, migration from WebGL, and when to use WebGPU, WebGL, or neither.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/WEBGPU_STUDY_GUIDE.md)

### WebSockets
This guide is a protocol-first, depth-first treatment of WebSockets for engineers building real-time features, with worked examples in both Node.js and Python. It starts on the wire — the HTTP upgrade handshake, frames, opcodes, close codes, `ws://` vs `wss://`, subprotocols — then builds up through the browser client API (events, binary data, reconnection with backoff), Node.js servers with `ws` (broadcasting, manual upgrade handling, heartbeats), Socket.IO (rooms, acknowledgements, and when not to use it), and Python servers (`websockets`, FastAPI/Starlette, Django Channels, python-socketio). It then goes deep on the hard parts: message-design patterns (envelopes, pub/sub, request-response, presence, framing), scaling stateful connections with a Redis backplane and sticky sessions, production concerns (the browser auth-header problem, Cross-Site WebSocket Hijacking, backpressure, heartbeats, reverse proxies), and reliability (delivery guarantees, at-least-once with acks/dedup, state recovery, the thundering herd). It closes with an honest comparison to SSE, long-polling, WebTransport, gRPC streaming, and WebRTC — including when *not* to use WebSockets — followed by five copy-paste recipes and an end-to-end walkthrough of a scalable, authenticated chat service.

[Open on GitHub](https://github.com/s4njee/larning/blob/main/WEBSOCKETS_STUDY_GUIDE.md)

## Notes

The GitHub links above point directly to the markdown files in the `main` branch of the public repository. If you want, I can also turn this README into a cleaner index with a table of contents, categories, or short tags for each topic.
